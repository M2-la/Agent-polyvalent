# ═══════════════════════════════════════════════
# Agent IA Polyvalent — Dépendances
# ═══════════════════════════════════════════════

# ── Interface chat ──────────────────────────────
chainlit>=2.0.0

# ── SDK Claude / Anthropic ──────────────────────
anthropic>=0.40.0

# ── Variables d'environnement ───────────────────
python-dotenv>=1.0.0

# ── Data manipulation ───────────────────────────
pandas>=2.2.0
polars>=0.20.0          # Plus rapide que pandas sur gros volumes
numpy>=1.26.0
openpyxl>=3.1.0         # Excel .xlsx
xlrd>=2.0.1             # Excel .xls legacy
pyarrow>=14.0.0         # Parquet + Arrow (requis par polars)
fastparquet>=2023.10.0  # Parquet alternatif

# ── Bases de données ────────────────────────────
duckdb>=0.10.0           # ★ Analytics OLAP : SQL sur CSV/Parquet/JSON, in-memory
psycopg2-binary>=2.9.9  # PostgreSQL
pymysql>=1.1.0           # MySQL / MariaDB
pymongo>=4.6.0           # MongoDB
sqlalchemy>=2.0.0        # ORM universel (PostgreSQL, MySQL, SQLite)
aiosqlite>=0.20.0        # SQLite async

# ── Graphiques / Visualisation ──────────────────
plotly>=5.18.0           # ★ Interactif (dashboards, web)
matplotlib>=3.8.0        # ★ Statique (publication, PDF)
seaborn>=0.13.0          # ★ Statistiques (distributions, corrélations)
kaleido>=0.2.1           # Export PNG/SVG depuis Plotly

# ── Export / Rapports ───────────────────────────
fpdf2>=2.7.0             # PDF simple
reportlab>=4.1.0         # PDF avancé (tableaux, mise en page)
jinja2>=3.1.2            # Templates HTML
weasyprint>=60.0         # HTML → PDF haute qualité (optionnel)
openpyxl>=3.1.0          # Écrire Excel

# ── Requêtes HTTP / API ─────────────────────────
aiohttp>=3.9.0
requests>=2.31.0
httpx>=0.27.0            # Async HTTP moderne

# ── Gmail / Email ───────────────────────────────
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.108.0

# ── Planification ───────────────────────────────
apscheduler>=3.10.0

# ── Utilitaires ─────────────────────────────────
aiofiles>=23.2.0
rich>=13.7.0             # Affichage terminal enrichi
tabulate>=0.9.0          # Tableaux ASCII (logs)
